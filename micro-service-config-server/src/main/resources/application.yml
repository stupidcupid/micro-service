spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/stupidcupid/micro-service
          search-paths: micro-service-config-repo
          username: ############
          password: ############
  profiles:
    active: dev
server:
  port: 7001
