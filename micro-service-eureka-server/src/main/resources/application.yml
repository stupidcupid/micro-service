server:
  port: 8761
eureka:
  client:
     ## 注册中心  不向注册中心注册自己
     register-with-eureka: false
     ## 注册中心维护服务实例 不需要检索服务
     fetch-registry: false
service-url:
      defaultZone: http://localhost:8761/eureka